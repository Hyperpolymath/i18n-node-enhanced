== Post-audit Status Report: polyglot-i18n

* *Date:* 2026-04-15
* *Status:* Complete (M5 Sweep)
* *Repo:* /var/mnt/eclipse/repos/polyglot-i18n

=== Actions Taken

[arabic]
. Standard CI/Workflow Sweep: Added blocker workflows
(`+ts-blocker.yml+`, `+npm-bun-blocker.yml+`) and updated `+Justfile+`.
. SCM-to-A2ML Migration: Staged and committed deletions of legacy
`+.scm+` files.
. Lockfile Sweep: Generated and tracked missing lockfiles where
manifests were present.
. Static Analysis: Verified with `+panic-attack assail+`.

=== Findings Summary

* 14 TODO/FIXME/HACK markers in contractiles/k9/template-hunt.k9.ncl
* 1 HTTP (non-HTTPS) URLs in adapters/crm/salesforce.js
* 1 JSON.parse call(s) with 0 try block(s) in audit/forensics.js —
JSON.parse throws SyntaxError on malformed input; wrap in try-catch
* 4 JSON.parse call(s) with 0 try block(s) in examples/fastify/test.js —
JSON.parse throws SyntaxError on malformed input; wrap in try-catch
* 1 JSON.parse call(s) with 0 try block(s) in
examples/node-http-autoreload/test.js — JSON.parse throws SyntaxError on
malformed input; wrap in try-catch
* 2 JSON.parse call(s) with 0 try block(s) in
examples/testlib/visitrest.js — JSON.parse throws SyntaxError on
malformed input; wrap in try-catch
* 2 JSON3.read/JSON.parse call(s) with 1 try block(s) in
julia/src/LanguageCladogram.jl — these throw on malformed input; wrap in
try/catch
* 27 unsafe get calls in src/core/FuzzyMatch.res
* 1 unsafe get calls in src/core/Segmenter.res
* 27 unsafe get calls in src/lib/bs/core/FuzzyMatch.res
* 1 unsafe get calls in src/lib/bs/core/Segmenter.res
* 27 unsafe get calls in src/lib/ocaml/FuzzyMatch.res
* 1 unsafe get calls in src/lib/ocaml/Segmenter.res
* 1 JSON.parse call(s) with 0 try block(s) in test/i18n.makePlurals.js —
JSON.parse throws SyntaxError on malformed input; wrap in try-catch
* 1 JSON.parse call(s) with 0 try block(s) in
test/i18n.writenewPhrase.js — JSON.parse throws SyntaxError on malformed
input; wrap in try-catch
* flake.nix declares inputs without narHash, rev pinning, or sibling
flake.lock — dependency revision is unpinned in flake.nix
* 1 import map entry/ies in deno.json without a version pin — specifiers
are not reproducibly resolved

=== Final Grade

* *CRG Grade:* D (Promoted from E/X) - CI and lockfiles are in place.
